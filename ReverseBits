class Solution(object):
    def reverseBits(self, n):
        reverse = ((bin(n)[2:]).zfill(32))[::-1]

        return int(reverse,2)
        







#First we need to convert integer to binary form. lengthen it to 32 bits.
#reverse it and convert it back to integer
#return the result integer
#Time complexity would be O(1), always a length of 32
#Space complexity will be O(1) as well. only 1 string variable for length of a fixed number 32.
